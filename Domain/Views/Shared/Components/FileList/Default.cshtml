@model IEnumerable<DocumentLinkVM>
@using WebCommon.Extensions
@using Domain.Extensions
@if (Model.Count() > 0)
{
    <div class="row">
        <div class="col-md-12">
            <table class="table table-bordered table--documents table--stripped">
                <tbody>

                    @foreach (var file in Model)
                    {
                        <tr>
                            <td style="width:55px;">
                                <div class="icon">
                                    <img src="@file.FileName.GetFileExtensionImage()" alt="">
                                </div>
                            </td>
                            <td>
                                <a href="@Url.Action("DownloadFile", "FileCDN", new { id = file.FileId })">
                                    @Html.Raw(file.FileTitle ?? file.FileName)
                                </a>
                            </td>
                        </tr>
                    }

                </tbody>
            </table>
        </div>
    </div>
}
