@model IEnumerable<StrategicDocumentVM>
@using WebCommon.Extensions
@if (Model.Count() > 0)
{
    @foreach (var item in Model)
    {
        item.CategoryImagePath = item.CategoryImagePath.EmptyToNull();
        <div class="fixed-item">
            <a asp-action="View" asp-controller="StrategicDocument" asp-route-id="@item.Id">
                <div class="image">
                    <img src="~/img/categories/@(item.CategoryImagePath ?? "icon_default.png")" alt="@item.CategoryText">
                </div>
                <p class="description">
                    @Html.Raw(item.Title)
                </p>
            </a>
        </div>
    }
}
else
{
    <span>Няма данни.</span>
}