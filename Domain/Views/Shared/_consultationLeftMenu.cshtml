@model List<MenuLinkViewModel>
<div class="aside row">
    <ul class="list--default">
        @foreach (var item in Model)
        {
            <li>
                <a href="@item.Url" target="_blank">
                    @Html.Raw(item.Title)
                </a>
            </li>
        }
    </ul>
</div>
